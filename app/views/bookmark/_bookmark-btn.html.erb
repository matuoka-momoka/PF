<% if mensore.bookmarkd_by?(current_user) %>
<%# if book.favorited_by?(current_user) %>
  <%= link_to mensore.bookmarks.count, mensore_bookmarks_path(mensore), method: :delete, remote: true, class: "glyphicon glyphicon-heart" %>
<% else %>
  <%= link_to mensore.bookmarks.count, mensore_bookmarks_path(mensore), method: :post, remote: true, class: "glyphicon glyphicon-heart-empty" %>
<% end %>