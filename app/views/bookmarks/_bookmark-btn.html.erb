<% if mensore.bookmarked_by?(current_user) %>
 <%= link_to mensore.bookmarkd_users.count, bookmarks_path(mensore), method: :delete, remote: true, class: "glyphicon glyphicon-heart" %>
<% else %>
  <%= link_to mensore.bookmarkd_users.count, bookmark_path(mensore), method: :post, remote: true, class: "glyphicon glyphicon-heart-empty" %>
<% end %>
