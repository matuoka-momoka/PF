<% if mensore.bookmarked_by?(current_user) %>
 <%= link_to mensore.bookmarks.count, mensore_bookmark_path(mensore_id: mensore.id, id: bookmark.id), method: :delete, remote: true, class: "glyphicon glyphicon-heart" %>
<% else %>
  <%= link_to mensore.bookmarks.count, mensore_bookmarks_path(mensore), method: :post, remote: true, class: "glyphicon glyphicon-heart-empty" %>
<% end %>
